const sqlite = require('better-sqlite3') // virdbirus simulator 2026 edition
const db = new Database('database.db')

// payment thing, but it store log (cough cough)

db.exec(`CREATE TABLE IF NOT EXISTS `)
 